<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:animateLayoutChanges="true"
    >
    <TextView
        android:id="@+id/courses_selection_title"
        style="@style/AppTheme.TextView.page_title"
        android:paddingRight="55dp"
        android:text="@string/select_course"
        android:layout_alignParentTop="true"
        />
    <android.support.v7.widget.SearchView
        android:id="@+id/courses_selection_search_view"
        android:layout_marginTop="@dimen/m20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:queryHint="Search Course"
        android:layout_below="@id/courses_selection_title"
        android:queryBackground="@color/app_background"
        android:background="@color/text_lightest_grey"
        />
    <TextView
        android:id="@id/empty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:textSize="20sp"
        android:visibility="gone"
        />
    <ImageButton
        android:id="@+id/course_selection_finish_button"
        style="@style/Widget.AppCompat.ImageButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/m10dp"
        android:layout_alignParentRight="true"
        app:srcCompat="@drawable/ic_arrow_forward"
        android:background="@color/primary_orange"
        android:contentDescription="Click to save course and move to next page"
        android:layout_alignParentBottom="true"
        android:visibility="gone"
        />
    <android.support.v7.widget.RecyclerView
        android:id="@+id/courses_selection_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/courses_selection_search_view"
        android:layout_above="@id/course_selection_finish_button"
        />
</RelativeLayout>